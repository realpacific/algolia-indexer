# Algolia Indexer

Read the document from file and upload to Algolia index

## Usage

```shell
node index.js \
   --appId {appId} \
   --apiKey {apiKey} \
   --fileLocation {path/to/json} \
   --index {indexName}
```